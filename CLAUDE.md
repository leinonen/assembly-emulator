# assembly emulator

- complete x86 feature set
- emulate the x86 assembly code
- written in Go
- support for VGA video memory programming (mode 13h), use a graphics library for this, so we can render the graphics.
- should be able to write small graphics programs that writes to video memory in asm
